# Feature Provenance & Leakage-Timing Table

Generated by `climate_health.features.climate.render_provenance_markdown()` (Stage 8). Covers every provided and derived climate feature: reference period, availability at prediction time, leakage risk, and notes.

| Feature | Reference period | Available at prediction time? | Risk | Notes |
|---|---|---|---|---|
| `rain_sum_7d` | T-7d -> T | Yes (as provided) | Low |  |
| `rain_sum_30d` | T-30d -> T | Yes (as provided) | Low |  |
| `rain_sum_90d` | T-90d -> T | Yes (as provided) | Low |  |
| `rain_days_30d` | T-30d -> T | Yes (as provided) | Low |  |
| `max_daily_rain_30d` | T-30d -> T | Yes (as provided) | Low |  |
| `tavg_7d` | T-7d -> T | Yes (as provided) | Low |  |
| `tavg_30d` | T-30d -> T | Yes (as provided) | Low |  |
| `tavg_90d` | T-90d -> T | Yes (as provided) | Low |  |
| `tmax_30d` | T-30d -> T | Yes (as provided) | Low |  |
| `tmin_30d` | T-30d -> T | Yes (as provided) | Low |  |
| `temp_range_mean_30d` | T-30d -> T | Yes (as provided) | Low |  |
| `ndvi_30d` | T-30d (satellite composite, may lag) | Needs verification | Medium | MODIS composites can have real-world processing/publication lag; fine for competition use as delivered, flagged for the Stage 19 deployment boundary. |
| `ndvi_90d` | T-90d (satellite composite, may lag) | Needs verification | Medium | Same lag caveat as ndvi_30d. |
| `elevation` | Static (terrain) | Yes | Low |  |
| `slope` | Static (terrain) | Yes | Low |  |
| `hot_days_30d` | T-30d -> T | N/A — dropped | N/A | Confirmed constant (0) across all 4,176 rows (Stage 3 forensics, re-verified by drop_dead_columns at every pipeline run). Dropped by Track A. |
| `rain_rate_7d / 30d / 90d` | same as source window | Yes (pure arithmetic of provided columns) | Low |  |
| `rain_ratio_acute_medium` | T-7d & T-30d | Yes | Low | NaN when rain_sum_30d == 0 (4/3,146 training rows) — genuinely undefined, not imputed to 0. |
| `rain_ratio_medium_chronic` | T-30d & T-90d | Yes | Low | rain_sum_90d is never 0 in the observed data, but the ratio is still NaN-safe if that changes. |
| `rain_intensity_30d` | T-30d | Yes | Low | NaN when rain_sum_30d == 0. |
| `rain_day_fraction_30d` | T-30d | Yes | Low |  |
| `tmax_30d_exceeds_flag / _exceedance_margin` | T-30d | Yes, but threshold is a train-only-fit constant | Low | Threshold (compute_heat_threshold) must be fit once on training data and reused verbatim for test/production — never recomputed per-dataset. |
| `{col}_anomaly (temperature)` | T-30d/90d vs. a learned train-only baseline | Yes, but the baseline must be the one learned from training, never recomputed from the batch being scored | Medium | See ClimateAnomalyFeaturizer docstring for the 3-tier fallback (group x month -> group -> global) and why it must be fit-on-train-only. fit_transform() (used to build this feature on the training set itself) uses leave-one-out bucket means so a training row's own value never inflates its own baseline; transform() on any other dataset (test, production) is unaffected since those rows never contributed to the baseline in the first place. |
| `{col}_anomaly (rainfall)` | T-30d/90d vs. a learned train-only baseline | Yes, same caveat as the temperature anomaly | Medium | Same leave-one-out fit_transform() behavior as the temperature anomaly. |
| `consecutive dry-spell length` | n/a — not implemented | N/A | N/A | NOT computable from the provided aggregates: rain_days_30d is a count of rainy days in the window, not their positions, so two windows with the same count can have very different spell structure. Documented as a known limitation rather than shipped as a feature that looks like it measures something it doesn't. |
| `ndvi_trend_30_90` | T-30d & T-90d (satellite composite, may lag) | Needs verification | Medium | Inherits both underlying columns' composite-lag caveat. |
